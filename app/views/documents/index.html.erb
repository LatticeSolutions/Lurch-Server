<% content_for :title, "Documents" %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Documents</h1>
    <%= link_to "New document", new_document_path, class: "rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white block font-medium" %>
  </div>

  <div id="documents" class="min-w-full divide-y divide-gray-200 space-y-5 my-4">
    <% if @documents.any? %>
      <ul class="list-disc ml-6">
      <% @documents.each do |document| %>
        <li>
          <%= document.title %>
          <% if document.user != current_user %>
            <span class="text-sm text-gray-600">(<%= document.user.email %>)</span>
          <% end %>
          <% if document.published? %>
            <span class="text-sm text-gray-600">(Public)</span>
          <% end %>
          <%= link_to "View", document, class: "text-sky-600 hover:underline mx-4", data: { turbo: false } %>
          <% if can? :publish, document %>
            <% if document.published? %>
              <%= button_to "Unpublish", unpublish_document_path(document), method: :patch, class: "text-sky-600 hover:underline cursor-pointer mr-4", form_class: "inline-block" %>
            <% else %>
              <%= button_to "Publish", publish_document_path(document), method: :patch, class: "text-sky-600 hover:underline cursor-pointer mr-4", form_class: "inline-block" %>
            <% end %>
          <% end %>
          <% if can? :destroy, document %>
            <%= button_to "Destroy", document, method: :delete, class: "text-red-600 hover:underline cursor-pointer", form_class: "inline-block", data: { turbo_confirm: "Are you sure?" } %>
          <% end %>
        </li>
      <% end %>
      </ul>
    <% else %>
      <p class="text-center my-10">No documents found.</p>
    <% end %>
  </div>
