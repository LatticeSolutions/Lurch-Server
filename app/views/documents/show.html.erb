<% content_for :title, @document.title %>

<link rel="stylesheet" href="/lurchmath/main-app-styles.css">

<h1 class="my-5 text-xl font-thin">
  <%= @document.title %>
  <% if @document.user != current_user %>
    <span class="text-lg text-gray-600">(<%= @document.user.email %>)</span>
  <% end %>
  <% if @document.published? %>
    <span class="text-lg text-gray-600">(Public)</span>
  <% end %>
</h1>
<%= content_tag :div, "Loading editor...",
      id: "editor-container",
      data: {
        controller: "document-editor",
        document_editor_id_value: @document.id,
        document_editor_title_value: @document.title,
        document_editor_can_edit_value: current_user.admin? || @document.user == current_user,
        document_editor_content_value: @document.content,
        document_editor_context_value: @document.context_documents.map { |d|
          { id: d.id, title: d.title, owner: d.user.email, content: d.content }
        }
      } %>

<script type="module" src="/lurchmath/editor.js"></script>
