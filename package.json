{
  "name": "app",
  "private": true,
  "devDependencies": {
    "esbuild": "^0.28.2"
  },
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --sourcemap --format=esm --outdir=app/assets/builds --public-path=/assets --external:/lurchmath/*",
    "postinstall": "node bin/vendor-lurch.mjs"
  },
  "dependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "@hotwired/turbo-rails": "^8.0.23"
  },
  "lurchSource": {
    "comment": "public/lde and public/lurchmath are fetched from this commit by bin/vendor-lurch.mjs (via the postinstall script), not committed to git. Bump `commit` to pull in upstream changes.",
    "repo": "kenmonks/lurch",
    "commit": "103fb6bc1f2e5e54254cad811a71cc3ad76b57a9"
  }
}
